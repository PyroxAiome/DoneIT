# Database connection string (REQUIRED)
DATABASE_URL=postgresql://doneit_user:your_secure_password@localhost:5432/doneit

# JWT Secret (optional, defaults to built-in secret)
JWT_SECRET=doneit-super-secret-key-2026

# Server port (optional, defaults to 3000)
PORT=3000
